# https://docs.espressif.com/projects/esp8266-rtos-sdk/en/latest/api-guides/build-system.html
# set(COMPONENT_NAME "dht11") # default behaviur is to use root dir name
# set(COMPONENT_SRCDIRS "include") # default behaviour is to include everything
# set(COMPONENT_ADD_INCLUDEDIRS "include") # default behaviour is include

register_component()